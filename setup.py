from setuptools import find_packages, setup

setup(
    name="animalistic",
    version="0.1",
    description="Track birds and other animals",
    platforms=["linux"],
    packages=find_packages()
)
